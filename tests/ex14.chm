struct User {
    first: int
    last: int
}

fun main(): int [
    marty = alloc User {
        first = 77
        last = 72
    }
    putchar(marty.first)
    putchar(marty.last)
    putchar(10)
    free marty
    return 0
]