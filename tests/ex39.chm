fun main(): int [
    if gt_zero(5) [
        putchar('a')
    ] else [
        putchar('b')
    ]
    return 0
]

fun gt_zero(n:int): bool [
    if n > 0 [
        return true
    ] else [
        return false
    ]
]