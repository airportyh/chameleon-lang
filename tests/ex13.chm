struct User {
    firstInitial: int
    lastInitial: int
}

fun printUser(user: User) [
    putchar(user.firstInitial)
    putchar(user.lastInitial)
    putchar(10)
]

fun main(): int [
    user = User {
        firstInitial = 84
        lastInitial = 72
    }
    user2 = User {
        firstInitial = 84
        lastInitial = 72
    }
    printUser(user)
    return 0
]